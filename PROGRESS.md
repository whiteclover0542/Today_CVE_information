# 오늘의 정보판 — 진행 관리

> 요구사항 원문은 [ASSIGNMENT.md](ASSIGNMENT.md)에 동결되어 있습니다. 이 문서는 진행 상황을 계속 갱신하는 관리 문서입니다.

## 진행 상태 (22개 작업)
✅ 완료 · 🔄 진행중 · ⬜ 미완료

| # | 작업 | 상태 | 비고 |
|---|------|------|------|
| 1 | 데이터 주제 선정 | ✅ | NVD 오늘의 신규 CVE 등록 건수(보안 테마, 무키 접근) |
| 2 | 아키텍처 설계 | ✅ | 정적 사이트 + 일 1회 GitHub Actions 배치, 서버리스 프록시 없음 |
| 3 | NVD API 무키 호출 스모크 테스트 | ✅ | 2026-08-22 실제 조회: `totalResults: 239`, HTTP 200 |
| 4 | 저장소 초기 구조 생성 | ✅ | `index.html`, `assets/`, `data/`, `scripts/`, `.github/workflows/` |
| 5 | `data/history.json` 스키마 정의 | ✅ | `{date, count, unit, timezone, sourceApiUrl, queriedAtUtc, severity:{...}, highlights:[{id,severity,summaryEn,summaryKo,url}]}` 배열 |
| 6 | 일일 워크플로 작성 | ✅ | `daily-cve-count.yml`, KST 09:00 cron, 날짜 중복 방지(스크립트가 존재 시 skip) |
| 7 | 워크플로 수동 실행 → Day 1 기록 확보 | ✅ | 2026-08-23 = 187건. 로컬 실행(184건)으로 1차 확보 후, 실제 쓰기 경로 검증을 위해 기록을 비우고 재실행 → Actions가 NVD 재조회·커밋(`08699ae`)·push까지 전부 성공 확인. 스킵(중복방지)·쓰기(신규커밋) 두 경로 모두 원격에서 검증 완료 |
| 8 | GitHub Pages 활성화 및 공개 주소 확정 | ✅ | `https://whiteclover0542.github.io/today_information/` 200 OK 확인 |
| 9 | 프론트엔드 렌더링 구현 | ✅ | 값·단위·출처·조회 시각·날짜별 기록 표·심각도 분포 그래프, 검정+흰색 테마로 재정비, 헤드리스 브라우저로 렌더 확인 |
| 10 | 목적 문장 + 출처/항목/단위/시간대 정의표 작성 | ✅ | `docs/worksheet/definitions.md`, Day1 원자료-화면값 대조 포함 |
| 11 | 출처 링크 동작 확인 | ✅ | href가 실제 NVD API 요청 URL로 연결됨을 확인 |
| 12 | 비밀값 노출 전체 검색 | ✅ | 워킹트리+`git log -p --all` 검색 결과 0건. 유일한 매치는 커밋 작성자 이메일(표준 git 메타데이터, 문제 아님) |
| 13 | 장애 시연 패널(`?debug=1`) 구현 | ✅ | 헤드리스 브라우저로 5개 버튼 노출 확인 |
| 14 | 장애 5종 개별 구현·테스트 | ✅ | 5종 모두 다른 배너 문구, 마지막 정상값(184건) 유지, 다시 시도로 복구 확인. **테스트 중 실제 버그 2건 발견·수정** (아래 설계 결정 이력) |
| 15 | 장애 재현 표 작성 | ✅ | `docs/worksheet/failure-matrix.md`, 빈 상태(정상값 없음) 실제 테스트 포함 |
| 16 | Day 2 기록 자동 확보 확인 | ⬜ | 날짜 넘어간 후, 중복 방지도 함께 확인 |
| 17 | 카드5 비교 로직 구현 | 🔄 | `app.js`의 `renderCompare()`로 구현 완료(기록 1건일 땐 비교 카드 숨김까지 확인). 실제 2개 날짜 데이터로 화면 검증은 Day2 확보 후 |
| 18 | 손계산 대조표 작성·화면값 대조 | ⬜ | 원자료/저장값/계산값/화면값 |
| 19 | 여러 브라우저 최종 확인 | ⬜ | Chrome·Edge 등 |
| 20 | 검증 안내서 작성 (이 문서 "검증 안내서" 섹션) | 🔄 | 카드1~3 기준 확정, 2×2 그리드 개편 반영 완료. 카드4·5 문구는 Day2 확보 후 최종 확인 |
| 21 | AI 3줄 작성 (이 문서 "AI 3줄" 섹션) | 🔄 | 세션 전체(주제 선정~시각화 재작업 3회) 반영해 다듬음 — 본인 표현으로 최종 확정 필요 |
| 22 | 최종 제출 화면 정리 | ⬜ | 아래 두 섹션만 제출 화면에 붙여넣기 |

---

## ⚠️ 항상 지킬 것 (매 커밋 전 확인)
GitHub Pages 무료 사용 조건 = 저장소 **Public** → 커밋 기록 전체가 공개됨. 나중에 파일에서 지워도 한 번 커밋된 개인정보·비밀값은 기록에 남아 계속 공개된다.

- [ ] 이름·이메일·전화번호 등 비공개 정보가 이 커밋에 없다
- [ ] 비밀번호·API 키·토큰이 이 커밋에 없다 (기본 경로는 애초에 키를 안 쓰지만, 실수로 다른 값이 섞이지 않았는지 확인)
- 실수로 들어갔다면: 삭제 커밋만으로는 부족(기록에 남음) → 즉시 `git filter-branch`/BFG로 히스토리 재작성 후 force-push로 조치. 애초에 넣지 않는 게 최선.

---

## 과제 개요
실제로 변하는 데이터 한 가지(오늘 KST 기준 NVD 신규 CVE 등록 건수)를 값·단위·출처·조회 시각과 함께 보여주는 개인 정보판. 현재값만 꾸미는 데서 끝내지 않고 출처·단위·시각·장애 상태·날짜별 기록으로 정보의 신뢰도를 설명한다. (원문: [ASSIGNMENT.md](ASSIGNMENT.md))

## 지향점
- 실제로 값이 바뀌는 데이터, 실제 장애 재현, 실제 날짜 경과가 원칙 — 가짜 데이터·가짜 타임스탬프 금지
- 값이 없을 땐 없는 대로 보여준다(위조 금지)
- 비밀값은 애초에 안 만든다(무키 경로 우선) — 방어적으로 최선
- AI가 만든 API 스펙·수치는 공식 문서로 직접 검증 후 반영한다(레이트리밋 등은 이미 확인 완료, 아래 부록 참고)

## 최종 제출물 (2개)
별도 파일(`docs/VERIFICATION.md`, `docs/AI_NOTES.md`) 대신 이 문서 안의 "검증 안내서"·"AI 3줄" 섹션에 바로 작성한다. 제출 화면에는 그 두 섹션의 내용만 옮겨 붙인다 — 나머지(체크리스트, 설계 이력, 부록 등)는 작업 기록용으로 이 문서에만 남는다.

---

## 과제 안내 (5단계)

### 1단계 — 값의 맥락 ⬜
- 하는 일: NVD에서 오늘(KST) 신규 등록 CVE 건수를 가져와 값·단위·출처·조회 시각을 한 화면에 표시
- 첫 행동: 목적 문장 채우기("이 정보판은 오늘 새로 등록된 보안 취약점 수를 확인하기 위한 것이다") → 출처·항목·단위·시간대 정의표 작성
- 통과 기준: 공개 주소에서 현재값·단위·출처·조회 시각이 한 화면에 보인다
- 막힐 때: 값의 의미·시각이 불분명 → 원자료 항목명·단위 확인, 자료 갱신 시각과 내가 조회한 시각을 구분
- 남길 것: 정의표, 원자료-화면값 대조 기록

### 2단계 — 비밀키와 호출 경로 ⬜
- 하는 일: 무키 경로로 NVD를 호출하고, 출처 주소를 화면에 노출해 클릭 시 원자료가 열리게 한다
- 첫 행동: 비밀값 없는 공개 출처 확정 → 소스·배포파일·네트워크주소·Git 기록에서 토큰·키 검색
- 통과 기준: 출처 주소가 화면에 보이고 클릭하면 원자료 페이지가 열린다
- 막힐 때: 비밀값 발견 시 → 값 들어간 파일 확인, 비밀값 없는 출처로 교체 후 전체 재검색
- 남길 것: 비밀값 0건 검사 기록, 데이터 호출 경로 그림

### 3단계 — 실패를 구분해 보여주기 ⬜
- 하는 일: timeout·인증 실패·호출 제한·오프라인·응답 형식 변경 5종을 각각 재현하고 서로 다르게 표시
- 첫 행동: 재현방법/화면문구/마지막정상값처리/다시시도행동 표 작성 → 하나씩 구현
- 통과 기준: 현재 자료인지 오래된 자료인지 상태 문구로 구분된다
- 막힐 때: 모든 장애가 같은 문구로 보임 → 오류 종류 분기·정상값 보존 로직 재확인
- 남길 것: 장애 5종 검사표, 각 상태·복구 화면 캡처

### 4단계 — 하루 한 번 기록 ⬜
- 하는 일: 기준 시간대(Asia/Seoul)를 명시하고 날짜별 값 하나만 저장, 같은 날 중복 방지
- 첫 행동: 저장 식별값을 "날짜(KST)+데이터종류"로 정하고 워크플로 작성 → Day 1 기록 즉시 확보
- 통과 기준: 날짜별 값이 같은 날짜 중복 없이 목록에 보인다
- 막힐 때: 다음 날짜를 기다리는 동안 2·3·5단계 문서·구현 작업을 먼저 끝낸다
- 남길 것: 서로 다른 실제 날짜 기록 2건, 능동 작업시간/경과기간 분리 기록

### 5단계 — 어제와 비교 검증 ⬜
- 하는 일: 두 날짜의 원자료·저장값·계산값·화면값을 대조하고 어제 대비 변화를 표시
- 첫 행동: 두 날짜 대조표 작성 → 손계산으로 차이·방향 계산 후 화면 결과와 비교
- 통과 기준: 이전 값과 현재 값의 차이·방향·단위가 한 화면에 보인다
- 막힐 때: 기록이 1건뿐이면 비교값을 표시하지 않는다(위조 금지)
- 남길 것: 원자료·저장값·계산값·화면값 대조표, 손계산 기록

---

## 마무리 체크리스트 (최종 검증 기준)

1. **현재값·단위·출처·조회 시각이 한 화면에 보인다**
   - 판단: 공개 주소 첫 화면에서 4가지가 스크롤 없이 확인됨
   - 증거: 배포된 화면 스크린샷
2. **화면값이 원자료의 같은 항목·단위·기준 시각과 일치한다**
   - 판단: NVD API 응답의 `totalResults`와 화면 숫자가 같은 시각 기준으로 일치
   - 증거: 원자료 응답 ↔ 화면값 대조 기록
3. **브라우저·배포 파일·네트워크 주소·Git 기록에 비밀값이 0건이다**
   - 판단: devtools 소스/네트워크, 정적 파일 전체, `git log -p` 전체 검색 결과 0건
   - 증거: 검색 결과 캡처/로그
4. **장애 5종이 구분되고 마지막 정상값·오래된 데이터 표시가 작동한다**
   - 판단: 5개 버튼 각각 다른 문구, 실패 중에도 마지막 정상값 유지, "오래된 데이터" 배지 표시
   - 증거: 장애 5종 검사표, 상태별 화면 캡처
5. **정상값이 없을 때 값이 있는 것처럼 표시하지 않는다**
   - 판단: 첫 로드 실패 시 빈 상태로 표시, 위조된 숫자 없음
   - 증거: 빈 상태 화면 캡처
6. **서로 다른 실제 날짜 기록 2건이 있고 같은 날 중복이 없다**
   - 판단: `history.json`에 날짜 키가 서로 다른 2개 이상, 같은 날 재실행해도 1건 유지
   - 증거: `history.json` 스냅샷, 워크플로 실행 로그
7. **기다린 기간과 능동 작업시간을 분리했다**
   - 판단: 대기 시간(자정 넘기기)과 실제 작업한 시간이 구분되어 기록됨
   - 증거: 작업 로그(이 문서의 진행 상태 갱신 이력)
8. **원자료·저장값·계산값·화면값을 손계산으로 재검산했다**
   - 판단: 두 날짜분 대조표에서 손계산 결과와 화면 결과가 일치
   - 증거: 손계산 대조표
9. **공개 화면·파일·제출 기록에 개인정보와 비밀값이 0건이다**
   - 판단: 8절 "항상 지킬 것" 체크리스트 통과
   - 증거: 최종 전체 검색 결과
10. **검증 안내서가 3단계 이내, 30초 안에 끝난다**
    - 판단: 어디로/무엇을/통과기준/안될때 4항목 모두 작성, 행동이 3단계 이하
    - 증거: 이 문서의 "검증 안내서" 섹션
11. **AI 3줄이 작성되었다**
    - 판단: 맡긴 일·판단한 일·안 들은 일 각 1줄
    - 증거: 이 문서의 "AI 3줄" 섹션

---

## 검증 안내서
> 🔄 카드1~3 문구 확정, 실사용 화면(2×2 그리드 개편) 반영 완료. 카드4·5 문구는 Day2 확보 후 최종 재확인 예정.

**어디로 가나요**
`https://whiteclover0542.github.io/today_information/` — 설치 없이 브라우저로 바로 열립니다.

**무엇을 하나요** (3단계)
1. 공개 주소 접속 → 오늘 건수·단위·출처·조회 시각과 심각도 분포가 스크롤 없이 한 화면에 보이는지 확인
2. "NVD CVE API 2.0 (원자료 열기)" 링크 클릭 → 화면의 숫자와 같은 값(`totalResults`)이 담긴 NVD 응답이 새 탭에 열리는지 확인
3. 주소 끝에 `?debug=1`을 붙여 재접속(예: `.../index.html?debug=1`) → 장애 버튼 5개 중 아무거나 눌러 상태 문구가 바뀌고 숫자는 그대로 남는지, "다시 시도"를 누르면 정상으로 돌아오는지 확인

**무엇이 보이면 통과인가요**
- 첫 화면에 오늘 건수·단위(건)·출처 링크·조회 시각이 스크롤 없이 보인다
- 원자료 링크를 누르면 화면의 숫자와 같은 값(`totalResults`)이 담긴 NVD 응답이 열린다
- 날짜별 기록에 서로 다른 날짜가 중복 없이 쌓여 있고, 최신 2개 날짜의 차이(±건수, 증가/감소)가 화면에 보인다
- `?debug=1`에서 장애 버튼 5개가 서로 다른 문구를 보여주고, 실패 중에도 마지막 정상값이 사라지지 않으며, "다시 시도"로 정상 상태로 돌아온다
- (덤) 오늘 등록분 심각도가 도넛 차트로, 날짜별 추이가 막대그래프로, 대표 CVE 몇 건이 실제 ID·설명·NVD 링크와 함께 보인다 — 통과 기준은 아니지만 신뢰도를 보태는 요소

**안 될 때**
- 값이 `—`로 보이고 빨간 배너만 뜬다 → 정상 데이터를 못 가져온 상태를 그대로 보여주는 것(숫자 위조 안 함). 새로고침 후에도 반복되면 GitHub Actions 실행 로그 확인
- 장애 버튼이 안 보인다 → 주소 끝에 `?debug=1`이 정확히 붙었는지 확인
- 날짜별 기록이 1건뿐이다 → 아직 두 번째 날짜가 안 쌓인 상태. 카드4/5 완료 후에는 2건 이상이어야 함(오너 확인 필요)

## AI 3줄
> 🔄 초안 — 제출 전 본인 표현으로 다듬어서 확정할 것

- **AI에게 맡긴 일**: 아키텍처 설계, 프론트엔드·워크플로·스크립트 코드 작성, 헤드리스 브라우저로 실제 클릭 테스트, 발견한 버그 수정, 화면 디자인 반복 작업, 문서화
- **내가 판단한 일**: 데이터 주제(보안 테마, NVD CVE 신규 등록 건수)와 무키 아키텍처 확정, 커밋·push·GitHub 설정 변경 시점마다 승인, 화면이 "보기 어렵다"고 세 번에 걸쳐 재작업을 지시(검정+흰색 테마 → 도넛 차트·2단 레이아웃 → 텍스트 축소·2×2 그리드), Day2를 기다리기 전에 Actions 쓰기 경로를 미리 검증하도록 결정
- **AI 말을 안 들은 일**: AI가 처음 제시한 예시성 데이터(날씨·환율 등) 대신 더 독창적인 보안 테마 데이터를 요구해서 다시 고르게 함, 작업 순서를 세세한 로드맵 대신 큰 틀의 진행 상태표로 바꾸라고 지시함, 그래프가 하나뿐이라 부족하다고 판단해 심각도 도넛 차트를 추가로 요구함

---

## 증거 보관 기준
- 제출 화면에는 위 "검증 안내서"·"AI 3줄" 두 섹션의 내용만 옮겨 붙인다.
- 검증 안내서는 공개 주소를 바로 열어 3단계 이내 행동으로 30초 안에 끝나게 쓴다.
- 평가자는 공개 주소의 현재 결과를 15초 안에 판정한다.
- 그 외 근거 자료(대조표, 검사표, 검색 로그 등)는 `docs/worksheet/`에 별도 보관하고 제출 화면에는 올리지 않는다.

---

## 남은 일
지금은 1단계(값의 맥락) 착수 전 — 위 "진행 상태" 표의 3~8번(무키 스모크 테스트 → 저장소 구조 → 워크플로 → Day 1 기록 → Pages 활성화)부터 순서대로 진행한다. 카드4 특성상 Day 1 기록을 최대한 빨리 확보하고, 그 대기 시간 동안 2·3단계 작업을 채운다.

---

## 설계 결정 이력
- **2026-08-23**: 데이터 주제를 예시(날씨·환율·버스·게임서버) 대신 보안 테마로 결정, "NVD 오늘의 신규 CVE 등록 건수"로 확정. 무키 호출 가능 + 실제 호출 제한(30초당 5회)이 있어 카드3의 근거로 인위적 연출 없이 쓸 수 있음.
- **2026-08-23**: 아키텍처를 "브라우저가 매번 NVD 직접 호출" 대신 "정적 사이트 + 일 1회 GitHub Actions 배치"로 결정. 정부 API의 CORS 허용 여부가 불확실해 서버리스 프록시가 추가로 필요할 리스크를 피하고, 카드2(비밀값 없음)도 가장 단순하게 만족시킴.
- **2026-08-23**: 헤드리스 브라우저(puppeteer-core + 로컬 Chrome)로 5종 장애 시나리오를 실제로 클릭해보다가 버그 2건 발견 후 수정.
  1. "응답 형식 변경" 시뮬레이션이 `text.slice(0, -1)`로 끝의 개행문자만 잘라내 여전히 유효한 JSON이 되는 바람에 실패가 재현되지 않음 → `text.trimEnd().slice(0, -1)`로 실제 닫는 괄호를 잘라내도록 수정.
  2. 정상 렌더링 시 상태 배너를 `hidden`만 처리하고 `textContent`는 지우지 않아 이전 오류 문구가 DOM에 남아있었음 → 정상 렌더링 시 배너 텍스트·클래스도 함께 초기화하도록 수정.
  두 버그 모두 "말로 설명"이 아니라 실제 클릭 테스트로만 드러났던 문제 — 카드3은 화면 캡처까지 재확인 완료.
- **2026-08-23**: Actions의 실제 커밋·push 경로가 한 번도 검증되지 않은 채 다음 날을 기다리는 게 불안해서, `data/history.json`을 잠시 비우고 workflow_dispatch를 재실행해 그 자리에서 검증. NVD를 실제로 재조회(184건→187건, 같은 KST 날짜 내 증가)해 커밋(`08699ae`)·push까지 성공 확인. 데이터는 그 순간의 진짜 NVD 응답이라 위조가 아니며, 내일 자동 cron이 처음 실패할 리스크를 오늘 미리 제거함.
- **2026-08-23**: 화면이 "보기 어렵다"는 피드백을 받아 (1) 카드 배경/테두리 대비, 여백, 아이콘을 더해 가독성을 높이고 (2) 기록이 1건뿐이어도 바로 볼 수 있는 시각화로 "오늘 등록분 심각도 분포"(CVSSv3 기준 스택 막대 + 범례)를 추가함. NVD의 `cvssV3Severity` 필터 파라미터로 CRITICAL/HIGH/MEDIUM/LOW 각각의 `totalResults`를 추가 조회(기존 요청과 합쳐 총 5회, 30초당 5회 제한과 정확히 같아 요청 사이 1.5초 간격을 둠)하고, 전체 건수에서 뺀 나머지를 "평가 대기"로 표시. 기존 187건 기록을 지우고 재조회해 208건(critical 2/high 11/medium 16/low 1/unrated 178)으로 다시 확보 — 역시 같은 날 재조회한 진짜 데이터.
- **2026-08-23 (5차)**: 대표 CVE 요약이 영어라 못 읽겠다는 피드백을 받아, 무키·무료 번역 서비스(MyMemory Translation API, `en|ko`, 익명 사용 하루 5000단어 한도)를 실제 호출로 검증 후 도입. NVD 호출(5회, 30초당 5회 제한)과는 별개 서비스라 영향 없고, 대표 CVE 3건만 번역하는 양이라 한도에도 여유 있음. 번역 실패 시 영문 요약으로 조용히 대체(치명적 오류 아님). `summary` 필드를 `summaryEn`/`summaryKo`로 분리하고 화면은 한국어를 우선 표시.
- **2026-08-23 (4차)**: "사이트에 더 추가할 거 없냐"는 질문에 "오늘의 대표 CVE 미리보기"를 추천해 채택. 이미 심각도별로 호출하던 4개 요청의 `resultsPerPage`를 1→3으로만 늘려(API 호출 횟수는 그대로 5회 유지, 요청당 페이로드만 약간 커짐) 건수뿐 아니라 실제 CVE 객체(id·설명·NVD 상세 링크)도 함께 받아옴. CRITICAL부터 순서대로 최대 3건을 뽑아 심각도 색점+ID(NVD 링크)+한 줄 요약으로 표시. 외부(NVD) 텍스트를 innerHTML로 꽂는 지점이라 escapeHtml을 통과시켜 XSS 여지를 없앰.
- **2026-08-23 (3차)**: "글이 너무 많고 세로 1열이라 불편하다"는 피드백을 받아 (1) "이게 뭔가요?" 두 문단을 `<details>` 한 줄 토글로 접고, 헤더 설명문도 한 문장으로 줄이고 (2) 카드 4개(값·심각도·추이·기록)를 세로로 쭉 쌓는 대신 데스크톱에서 2×2 그리드(`.grid-2`)로 배치해 스크롤 없이 한 화면에 다 보이게 함, 모바일에서는 자동으로 1열로 풀림. 대시보드 최대폭도 900→1040px로 넓힘.
- **2026-08-23 (2차)**: 그래도 "보기 어렵다"는 재요청을 받아 큰 폭으로 다시 손봄 — 심각도를 얇은 막대 대신 SVG 도넛 차트(중앙에 총 건수)로 바꾸고, 값 카드와 심각도 카드를 데스크톱에서는 2열(hero-grid), 모바일에서는 1열로 쌓이게 하고, 어제 대비 변화를 값 카드 안의 색상 배지(▲빨강 증가/▼초록 감소)로 옮기고, 추이 그래프가 없을 때는 빈 텍스트 대신 애니메이션 스켈레톤 막대를 보여주도록 함. 헤드리스 브라우저로 확인하다가 실제 버그 발견: `hidden` 속성이 있어도 같은 클래스에 `display` 값을 직접 지정하면(`.compare-inline{display:inline-flex}`, `.severity-card{display:flex}`) author 스타일이 UA의 `[hidden]{display:none}`을 명시도 동률에서 origin 우선순위로 이겨버려서 숨겨야 할 요소가 빈 채로 보임 → 전역 규칙 `[hidden]{display:none!important}`을 최상단에 추가해 해결.

---

## 부록: NVD API 스펙 (2026-08-23, 공식 문서로 확인 완료)

| 항목 | 값 |
| --- | --- |
| Base URL | `https://services.nvd.nist.gov/rest/json/cves/2.0` |
| 날짜 파라미터 | `pubStartDate`, `pubEndDate` — ISO-8601 확장형 `YYYY-MM-DDTHH:MM:SSZ` |
| 날짜 범위 제한 | 최대 연속 120일 |
| 건수 필드 | 응답의 `totalResults` |
| 심각도 필터 | `cvssV3Severity=CRITICAL\|HIGH\|MEDIUM\|LOW` (2026-08-23 실제 호출로 동작 확인) |
| 호출 제한 (키 없음) | 30초당 5회 |
| 호출 제한 (키 있음) | 30초당 50회 |

키는 기본 경로에서 아예 사용하지 않는다(카드2 원칙). 하루 1회 호출이라 무키 제한으로 충분.

## 부록: 아키텍처 개요

```
[GitHub Actions: 매일 00:00 UTC(=09:00 KST) 실행]
        │  NVD API 호출 (키 없이, pubStartDate/pubEndDate = 오늘 KST 00:00~실행 시각)
        ▼
  data/history.json 에 {date(KST), count, unit, sourceApiUrl, queriedAtUtc} 추가
  (해당 날짜 키가 이미 있으면 아무 것도 안 함 → 중복 방지)
        │  git commit & push
        ▼
[GitHub Pages: index.html + app.js]
        │  같은 저장소의 data/history.json을 fetch (동일 출처, CORS 없음)
        ▼
  화면 렌더링: 현재값(최신 기록)·단위·출처 링크·조회 시각
              어제 대비 변화(방향/크기/단위)
              장애 5종 시연 패널(?debug=1) — 프론트엔드에서 fetch를 감싸 인위 재현
```

## 부록: 파일 구조(안)

```
/
├── ASSIGNMENT.md          # 동결된 요구사항 (수정 금지)
├── PROGRESS.md            # 이 문서
├── index.html             # 정보판 화면
├── assets/
│   ├── app.js              # fetch, 렌더링, 장애 시뮬레이터, 어제 대비 계산
│   └── style.css
├── data/
│   └── history.json        # 날짜별(KST) 기록 — Actions가 추가, 앱이 읽기 전용으로 사용
├── .github/workflows/
│   └── daily-cve-count.yml # 매일 00:00 UTC 실행, NVD 조회 후 history.json 갱신·커밋
└── docs/
    └── worksheet/            # 손계산 대조표, 장애 5종 표, 비밀값 0건 점검 등 근거 자료

검증 안내서·AI 3줄은 별도 파일 없이 PROGRESS.md 안의 동명 섹션에 작성한다.
```

## 부록: 시간대·식별자 규칙
- 기준 시간대: **Asia/Seoul (KST, UTC+9)**, 예외 없이 이 기준만 사용
- 날짜 식별값: KST 기준 `YYYY-MM-DD` 문자열
- 워크플로 UTC cron `0 0 * * *` = KST 09:00 → 그 시각까지의 "오늘(KST) 누적 신규 CVE 건수"를 그 날짜의 기록으로 저장

## 부록: 배포
- 정적 파일: GitHub Pages (`main` 브랜치 또는 `gh-pages`, Actions로 자동 배포)
- 공개 주소: `https://whiteclover0542.github.io/today_information/` (2026-08-23 활성화 완료, 200 OK 확인)

## 부록: 열린 질문 / 리스크
- "오늘 하루 동안" 카운트가 0건이거나 NVD 쪽 지연으로 당일 데이터가 늦게 올라오는 날이 있을 수 있음 → "정상값 없음" 처리 기준을 워크플로에 명시할 것
- 카드4의 실제 날짜 2건 확보를 위해 최소 하루는 그냥 기다려야 함 — 대기는 작업시간에서 제외하고 별도로 기록
