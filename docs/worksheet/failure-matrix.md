# 장애 5종 검사표 (카드3 자체 점검 근거)

확인 방법: 공개 주소 뒤에 `?debug=1`을 붙여 접속(`https://whiteclover0542.github.io/today_information/index.html?debug=1`) → 장애 시연 패널 버튼 클릭.
검증 도구: 헤드리스 Chrome(puppeteer-core)으로 실제 클릭 후 DOM 상태 캡처(2026-08-23). 이후 2026-08-24, 배포 화면(13건 기준)에서 오너가 5개 버튼·다시 시도를 직접 클릭해 동일하게 재확인.

| # | 장애 종류 | 재현 방법 | 화면 문구 | 마지막 정상값 처리 | 다시 시도 행동 |
| --- | --- | --- | --- | --- | --- |
| 1 | timeout | `AbortController`로 실제 진행 중인 fetch를 즉시 `abort()`시켜 진짜 `AbortError` 발생 | "⏱ 요청이 시간 초과되었습니다 — 오래된 데이터 표시 중 (마지막 정상 조회: HH:MM:SS)" | 유지(184건 계속 표시) | "다시 시도" 클릭 → 정상 fetch 재실행 → 배너 사라지고 정상 상태 복구 |
| 2 | 인증 실패 | 요청 전 강제로 `HTTP 401` 상당의 오류를 던짐(모의) | "🔒 인증에 실패했습니다 — 오래된 데이터 표시 중 (마지막 정상 조회: HH:MM:SS)" | 유지 | 동일 |
| 3 | 호출 제한 | 요청 전 강제로 `HTTP 429` 상당의 오류를 던짐(모의) | "🚦 호출 제한에 걸렸습니다 (너무 많은 요청) — 오래된 데이터 표시 중 (마지막 정상 조회: HH:MM:SS)" | 유지 | 동일 |
| 4 | 오프라인 | 네트워크 요청 자체가 실패하는 상황(모의, `TypeError: Failed to fetch`와 동일 유형) | "📡 오프라인 상태입니다 — 오래된 데이터 표시 중 (마지막 정상 조회: HH:MM:SS)" | 유지 | 동일 |
| 5 | 응답 형식 변경 | 실제 `data/history.json`을 가져온 뒤 마지막 문자(닫는 괄호)를 잘라 실제로 `JSON.parse` 실패를 유발 | "⚠️ 응답 형식이 예상과 다릅니다 — 오래된 데이터 표시 중 (마지막 정상 조회: HH:MM:SS)" | 유지 | 동일 |

## 정상값이 아예 없는 경우(첫 로드 실패)
`lastGood`이 없을 때 실패가 발생하면 값은 `—`로 표시되고, 배너 클래스가 `empty`로 바뀌며 문구 끝이 "…— 아직 정상 데이터를 가져오지 못했습니다"로 달라진다(위조된 숫자를 보여주지 않음).

실제 검증: 로컬에서 `data/history.json`을 잠시 이름 바꿔 없앤 뒤 첫 로드 → `value-number`가 `—`, 배너가 `오류: HTTP 404 — 아직 정상 데이터를 가져오지 못했습니다`(class: `status-banner empty`)로 표시됨을 헤드리스 브라우저로 확인(2026-08-23). 확인 후 파일 원복.

## 참고
- 5종 모두 서로 다른 문구 + 아이콘으로 구분됨을 확인.
- timeout·응답 형식 변경은 실제 네트워크/파싱 오류를 그대로 재현(모의 데이터 아님). 인증 실패·호출 제한·오프라인은 NVD를 매일 1회만 호출하는 배치 구조상 실제로 발생시키기 어려워 코드 상에서 동일한 오류 처리 경로를 타도록 모의(labeled "모의")로 재현.
