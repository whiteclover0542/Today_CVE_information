# 오늘의 CVE 정보판 — 서비스 기획서

> 이 문서는 과제 완료 이후, 실제 서비스로 발전시키는 단계의 기획을 담습니다.
> 과제 당시 자료는 [`assignment/`](assignment/)에 그대로 남아 있고, 이 문서는 별도로 계속 갱신합니다.
> 진행 상황은 [`PROGRESS.md`](PROGRESS.md)에서 관리합니다.

## 1. 서비스 개요

오늘(KST) 새로 등록된 [NVD](https://nvd.nist.gov/) CVE(보안 취약점)를 건수·심각도·위험도·대표 사례·유형/제품별 분포·추이까지 한 화면에서 보여주는 보안 정보판입니다. 서버·로그인·설치 없이 브라우저로 바로 접근할 수 있는 정적 사이트를 지향합니다.

- **공개 주소**: https://whiteclover0542.github.io/Today_CVE_information/
- **저장소**: https://github.com/whiteclover0542/Today_CVE_information

## 2. 이번 단계의 목표

과제 통과를 위한 최소 기능은 이미 갖췄습니다. 이제부터는 실제로 사람이 매일 들어와 볼 만한 서비스로 다듬는 것이 목표이며, 이번 단계에서는 아래 세 가지에 집중합니다.

### 2-1. 배포 재설정

- 현재는 GitHub Pages(저장소 설정에서 활성화)로 배포되어 있습니다.
- 배포 파이프라인을 다시 점검·재설정합니다 — 목적은 이후 기능 추가(특히 LLM 호출 시크릿 관리)가 늘어나도 배포가 끊기지 않고, 실패했을 때 원인을 바로 알 수 있는 구조를 만드는 것입니다.
- 구체적인 방식(그대로 GitHub Pages 유지 vs. 다른 정적 호스팅으로 이전)은 착수 시점에 다시 판단하고, 결정 근거는 `PROGRESS.md`의 설계 결정 이력에 남깁니다.

### 2-2. LLM 기반 번역·요약 품질 개선

현재 상태(개선 전):

| 기능 | 현재 방식 | 한계 |
| --- | --- | --- |
| 대표 CVE 한국어 번역 | MyMemory Translation API (무료, 문장 단위 기계번역) | 보안 용어 오역, 문맥 손실, 실패 시 원문 그대로 노출 |
| "기타" 유형 재분류 | Gemini 무료 티어 (닫힌 목록 중 하나만 고르는 분류 전용 호출) | 분류에만 쓰이고, 요약·설명 생성에는 쓰이지 않음 |
| 오늘 등록분 전체 요약 | 없음 (개별 카드 나열뿐) | 하루치를 한눈에 이해할 수 있는 브리핑이 없음 |

개선 방향:

- 대표 CVE 번역을 MyMemory에서 LLM 기반 번역으로 교체해, 보안 도메인 용어(권한 상승, 인증 우회 등)를 더 자연스럽고 정확하게 옮깁니다.
- 오늘 등록분 전체를 짧게 요약하는 "오늘의 브리핑" 카드를 LLM으로 생성하는 방안을 검토합니다(예: 가장 눈에 띄는 경향, 주의할 유형/제품 한두 줄).
- LLM 호출은 지금과 마찬가지로 **배포 후 프론트엔드가 아니라 매일 실행되는 배치 스크립트(GitHub Actions)에서만** 수행하고, 키는 저장소 시크릿으로만 관리해 비밀값 노출 원칙을 유지합니다.
- 무료 티어 한도(요청 수·토큰 수)를 벗어나지 않도록 하루 1회 배치 호출 안에서 처리하고, 실패 시 원문/기존 방식으로 폴백하는 원칙(현재 번역·분류 로직과 동일)을 그대로 따릅니다.

### 2-3. 대표 CVE 노출 확대 및 심층 해설

현재 상태:

- 하루 대표 CVE가 3건으로 고정(`MAX_HIGHLIGHTS = 3`)돼 있고, 카드 하나당 CVSS 점수·유형 태그·공격 조건·번역문 정도만 보여줍니다.
- "이 취약점이 왜 생겼는지", "어떻게 막는지"에 대한 해설은 전혀 없어, 보안 지식이 없는 사람은 번역문만 봐서는 실무적으로 뭘 해야 하는지 알기 어렵습니다.

개선 방향:

- 노출 대상은 그날 등록된 CVE 전체가 아니라 **심각도 상위 CVE**로 제한합니다(CRITICAL → HIGH → MEDIUM 순으로 채우는 기존 대표 CVE 선정 방식을 그대로 확장). 정확한 상한 건수는 LLM 호출 한도·배치 실행 시간을 실측한 뒤 정합니다.
- 화면 노출 방식은 점진적 공개(progressive disclosure) 구조로 바꿉니다.
  - 화면에는 처음에 **1건만** 노출합니다.
  - **더보기** 버튼을 누르면 위에서 정한 심각도 상위 대상 전체를 볼 수 있습니다.
  - 노출 대상이 많을 때는 한 번에 다 펼치지 않고 **5건 단위로 페이지네이션**합니다.
- 노출되는 CVE마다 아래 내용을 함께 보여줍니다.
  - **설명과 해석**: 원문 번역 + 무슨 뜻인지 쉽게 풀어 쓴 요약
  - **발생 원인**: 이런 유형의 취약점이 왜 생기는지(가능하면 NVD가 제공하는 CWE(Common Weakness Enumeration) 분류를 근거로 삼아, LLM이 근거 없이 지어내지 않도록 함)
  - **방지·완화 방법**: 패치 적용, 설정 점검 등 일반적인 대응 가이드
- 구현 방향은 NVD 응답의 CWE 필드를 우선 추출해 근본 원인의 뼈대로 쓰고, 그 위에 LLM이 설명·원인 해설·완화 방법을 문장으로 다듬는 방식을 검토합니다. CWE 정보가 없는 CVE는 해설 없이 번역문까지만 보여주고, 있지도 않은 원인을 지어내지 않습니다(§3 원칙).
- **아직 정하지 않은 것**: 심각도 상위 몇 건까지를 상한으로 둘지(예: 10건, 20건 등)는 아직 미정입니다. 착수 시점에 LLM 호출 한도(무료 티어 요청 수)·배치 실행 시간을 실측해 상한을 정하고, 결정 근거는 `PROGRESS.md`에 남깁니다. 상한을 넘는 CVE는 해설 카드 없이 건수 집계(심각도·유형 분포 등 기존 기능)에만 반영됩니다.

## 3. 유지할 원칙

과제 단계에서 세운 원칙 중 실 서비스에서도 그대로 지킬 것들입니다.

- 비밀값은 브라우저·배포 파일·Git 기록 어디에도 남기지 않는다(무키 우선, 꼭 필요하면 배치 환경 시크릿으로만).
- 값이 없으면 없는 대로 보여준다(위조 금지) — LLM 요약도 생성 실패 시 카드를 숨기거나 기존 방식으로 대체하고, 없는 내용을 지어내지 않는다.
- 실패는 구분해서 보여준다 — LLM 호출 실패도 기존 장애 처리 원칙(마지막 정상값 유지, 오래된 데이터 표시)을 따른다.

## 4. 다음 단계 후보 (아직 착수 전)

우선순위·착수 여부는 미정이며, 아래 순서는 확정된 로드맵이 아닙니다.

- 배포 재설정
- LLM 기반 번역 교체
- 대표 CVE 노출 건수 확대 + 설명/원인/방지법 해설 카드
- LLM 기반 오늘의 브리핑 요약
- (검토) 번역/요약 품질에 대한 사람 검수 또는 샘플 비교 기록

## 5. 참고 문서

- [`PROGRESS.md`](PROGRESS.md) — 이 기획을 실제로 진행하며 갱신하는 상태표
- [`README.md`](README.md) — 현재 서비스의 기능·구조 설명
- [`assignment/`](assignment/) — 과제 원문·제출 문서·근거 자료(동결, 실 서비스 기획과 별도)
